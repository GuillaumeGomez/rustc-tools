searchState.loadedDescShard("rustc_macros", 0, "<code>HashStable</code> implementation that has no <code>HashStableContext</code> …\nDerives <code>TypeFoldable</code> for the annotated <code>struct</code> or <code>enum</code> (…\nDerives <code>TypeVisitable</code> for the annotated <code>struct</code> or <code>enum</code> (…\nDerive an extension trait for a given impl block. The …")